{
  "name": "shiritori-dojo",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "fetch-words": "node scripts/fetch-words.mjs",
    "serve": "npx serve .",
    "test": "node --test"
  },
  "dependencies": {
    "adm-zip": "^0.6.0"
  }
}
